module.exports = {
  // enable class-based dark mode so we control it by adding/removing `dark` on <html>
  darkMode: 'class',
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
}
